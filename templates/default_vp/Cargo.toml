[package]
name = "my-anoma-vp" # This will be replaced by the user
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
# Placeholder for the actual Anoma SDK crate
# anoma_sdk = "0.1.0" 